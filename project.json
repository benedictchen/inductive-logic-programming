{
  "name": "inductive_logic_programming",
  "root": "packages/inductive_logic_programming", 
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["python -m pip install build", "python -m build"],
        "cwd": "packages/inductive_logic_programming"
      }
    },
    "test": {
      "executor": "nx:run-commands", 
      "options": {
        "commands": ["python -m pytest . -v"],
        "cwd": "packages/inductive_logic_programming"
      }
    }
  }
}
